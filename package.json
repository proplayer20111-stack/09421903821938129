{
  "name": "calling-website",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check server.js && node --check public/app.js",
    "start": "node server.js"
  }
}
