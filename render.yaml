services:
  - type: web
    name: callroom
    runtime: node
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    healthCheckPath: "/healthz"
    autoDeploy: true
    envVars:
      - key: SITE_PASSWORD
        sync: false
