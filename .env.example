SITE_PASSWORD=Seth123
PORT=3000
# Optional: enables live GIF search in chat through GIPHY.
# GIPHY_API_KEY=
# Optional: stable Web Push keys. If omitted, temporary keys are generated on each server start.
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
# WEB_PUSH_CONTACT=mailto:you@example.com
# Optional: set DATA_DIR to store accounts/device rules outside the app folder.
# DATA_DIR=/var/data
