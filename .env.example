SITE_PASSWORD=Seth123
PORT=3000
# Optional: Neon pooled Postgres connection string for permanent account storage.
# DATABASE_URL=postgresql://user:password@host-pooler.region.aws.neon.tech/neondb?sslmode=require
# Optional but strongly recommended for calls across restrictive phone networks.
# TURN_URL=turn:your-turn-server.example.com:3478
# TURN_USERNAME=
# TURN_CREDENTIAL=
# Optional: enables live GIF search in chat through GIPHY.
# GIPHY_API_KEY=
# Optional: stable Web Push keys. If omitted, temporary keys are generated on each server start.
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
# WEB_PUSH_CONTACT=mailto:you@example.com
# Optional: set DATA_DIR to store accounts/device rules outside the app folder.
# DATA_DIR=/var/data
